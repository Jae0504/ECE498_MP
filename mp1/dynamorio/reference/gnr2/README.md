These CSVs are copied from the actual gnr2 experiment on 2026-09-09.
Native timings are observations, not expected constants or fabricated targets.
Miss rates are cold virtual-address cache simulations with a 256 MiB reference LLC,
not hardware cache counters. The real host had a 336 MiB LLC.
The deployment runner compares every reported metric and checks analytical quantities
and checksums; it does not force noisy observations to equal these values.
